# Search uses 3 different searching algorithms DFS, BFS, A*
## Usage
* Go to `src` folder
* run `javac *.java` to compile
* usage: `java Search input output`
    use `-` for command line inputs or outputs
